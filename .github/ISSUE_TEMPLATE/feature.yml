# GENERATED by `abcli feedback templates --sync` from the report contract
# (agents/feedback/report.py). Do not edit by hand — `--check` will fail, and the CLI door and
# this one would drift. Change REQUIRED/PROMPTS and re-sync.
name: "Capability request"
description: "Something the system cannot do yet"
labels: ["type:feature", "from:dev", "area:triage"]
body:
  - type: markdown
    attributes:
      value: |
        Tell us what you are trying to do, and what stopped you.
  - type: textarea
    id: motivation
    attributes:
      label: "Motivation"
      description: "why this capability is needed — the problem, not the solution"
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: "Proposal"
      description: "what the capability would look like"
    validations:
      required: true
